---
description: "Run the Bun + Next.js pre-PR validation gate (with compatibility fallbacks)"
---

Invoke the `plangate:gate` skill and follow it exactly as written. The user's arguments are: `$ARGUMENTS`
