---
description: "Create or resume an investigation document for a complex bug"
argument-hint: "[problem-name]"
disable-model-invocation: true
---

Invoke the `plangate:investigate` skill and follow it exactly as written. The user's arguments are: `$ARGUMENTS`
