---
description: "Phase lifecycle command: start N [description] or finish [N]"
argument-hint: "start N [desc] | finish [N]"
disable-model-invocation: true
---

Invoke the `plangate:phase` skill and follow it exactly as written. The user's arguments are: `$ARGUMENTS`
