---
description: "Execute PLAN.md tasks with sequential implementer -> build gate -> reviewer flow"
argument-hint: "[phase-number]"
disable-model-invocation: true
---

Invoke the `plangate:orchestrate` skill and follow it exactly as written. The user's arguments are: `$ARGUMENTS`
