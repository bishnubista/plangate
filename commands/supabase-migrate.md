---
description: "Run post-migration Supabase checks: regenerate types + security/performance advisors"
---

Invoke the `plangate:supabase-migrate` skill and follow it exactly as written. The user's arguments are: `$ARGUMENTS`
